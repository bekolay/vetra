@model IEnumerable<veri.Menuler>

@{
    ViewBag.Title = "Menüler";
}


<div class="container" style="background: #ffffff;border-top-width: 50px;padding-top: 50px;padding-bottom: 50px;">
    <h2>Menüler</h2>
    <br />
    <p>
        @Html.ActionLink("Menü Oluştur", "Create")
    </p>
    <hr />
    <br />
    <table id="datable_1" class="table table-hover display">
        <thead>
            <tr>
                <th>#</th>
                <th>Menü Adı</th>
                <th>Menü mü ?</th>
                <th>Kategori mi ?</th>
                <th>Aktif mi ?</th>
                <td></td>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@Html.DisplayFor(modelItem => item.Menulerid)</td>
                    <td>@Html.DisplayFor(modelItem => item.MenuAdi)</td>
                    <td>@Html.DisplayFor(modelItem => item.MenuMi)</td>
                    <td>@Html.DisplayFor(modelItem => item.KategoriMi)</td>
                    <td>@Html.DisplayFor(modelItem => item.MenuAktif)</td>
                    <td>
                        @Html.ActionLink("Düzenle", "Edit", new { id = item.Menulerid })
                    </td>

                </tr>
            }
        </tbody>
    </table>
</div>